/**
 * The variables to be plugged into pathName or fileName
 */
export enum Variables {
    ACTOR_NAME = '{actorName}',
    RUN_ID = '{runId}',
    DATE = '{date}',
    NOW = '{now}',
    UUID = '{uuid}',
    INCREMENTOR = '{incrementor}',
}
